# Dependencies
node_modules/
.pnpm-store/

# npm lockfile — bun.lock is canonical (AGENTS.md); npm installs are reference-only
package-lock.json

# Builds
.next/
out/
dist/
*.tsbuildinfo
.turbo/

# Environment — never commit real secrets
.env
.env.local
.env.*.local
!.env.example

# Testing
coverage/
test-results/
playwright-report/
/blob-report/
/playwright/.cache/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# Deployment
.vercel/

# SSH deploy keys — never commit private keys
ssh-key.txt
**/ssh-key.txt
*.key
**/*.key

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

server.log
# Skills directory
/skills/

# runtime sqlite database (contains customer PII — never commit)
/db/*.db
/db/*.db-journal
